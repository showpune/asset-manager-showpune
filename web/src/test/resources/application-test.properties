# Test configuration - do not set profiles here

# Use in-memory database for tests
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Use local storage for tests
azure.storage.account-name=test-account
azure.storage.endpoint=https://test-account.blob.core.windows.net
azure.storage.container-name=test-container